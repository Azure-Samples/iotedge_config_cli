## The following values will automatically be filled in when installing the configuration bundles generated by the iotedge_config_cli
## Leave this section out
#
# hostname = "192.168.0.180"
# parent_hostname = "192.168.0.129"
#
# [provisioning]
# source = "manual"
# iothub_hostname = ""
# device_id = ""
#
# [provisioning.authentication]
# method = "sas"
# device_id_pk = { value = "" }
#
# trust_bundle_cert = ""
#
# [edge_ca]
# cert = "<File URI path to the device full chain CA certificate unique to this device.>"
# pk = "<File URI path to the device CA private key unique to this device.>"
#
# [agent.config]
# image = "mcr.microsoft.com/azureiotedge-agent:1.2"


## Add other parameters that you want to be included in the configuration bundles here
[agent]
name = "edgeAgent"
type = "docker"

[connect]
workload_uri = "unix:///var/run/iotedge/workload.sock"
management_uri = "unix:///var/run/iotedge/mgmt.sock"

[listen]
workload_uri = "fd://aziot-edged.workload.socket"
management_uri = "fd://aziot-edged.mgmt.socket"

[moby_runtime]
uri = "unix:///var/run/docker.sock"
network = "azure-iot-edge"
