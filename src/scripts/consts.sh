# This script will attempt to configure a pre-installed iotedge as a nested node.
# it must be run as sudo, and will modify 

device_id={device_id:?}
hostname={hostname:?}
parent_hostname={parent_hostname:?}